<% flash.each do |type, message| %>
  <% color = type == "notice" ? "green" : "red" %>
  <div class="bg-<%= color %>-100 border border-<%= color %>-300 text-<%= color %>-800 px-4 py-3 rounded mb-4 mx-6 mt-6">
    <%= message %>
  </div>
<% end %>
