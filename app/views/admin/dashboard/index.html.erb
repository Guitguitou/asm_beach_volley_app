<div class="bg-white shadow rounded-lg p-6 mb-6">
  <h3 class="text-lg font-semibold text-asmbv-red mb-4">Derniers joueurs ajoutés</h3>
  <ul class="divide-y divide-gray-200">
    <% @recent_players.each do |player| %>
      <li class="py-2 text-sm">
        <%= player.full_name %> – <span class="text-gray-500"><%= player.phone %></span>
      </li>
    <% end %>
  </ul>
</div>
